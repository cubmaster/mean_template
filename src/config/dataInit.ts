//import * as UserFunctions from '/utilities/UserFunctions';

export class dataInit
{
    constructor() {

        this.MakeAdminUser();
    }



    MakeAdminUser() {

      // UserFunctions.UserCount.count().exec(function (err: any, count: number) {
      //     if (count == 0) {
      //         UserFunctions.CreateNewUser('admin', 'admin', 'admin', 'admin', 'admin',['admin'],
      //             function (user: any) {
      //                 console.log(user);
      //             });

      //     }
      // });
    }


}

